#include "acceleration_calibrator.h"

void dynamic_acceleration_calibrator() {
  // TODO actually calibrate:
  /*
   Check if we are currently NOT moving at constant speed.
   If so, be smart about the distance travelled since we last hit a sensor
  */
}
